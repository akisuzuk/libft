# /bin/bash
git config --global user.name akisuzuk
git config --global user.email 122352761+akisuzuk@users.noreply.github.com

# add commit pushとかの流れ
# git add .
# git commit -m "message"
# git push origin main（origni mainは省略可能）

# cloneの流れ
# git clone https://github.com/akisuzuk/libft.git libft
